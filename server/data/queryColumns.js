export const userColumns = `name`;
export const transactionsColumns = 't.accountType, t.accountFrom, t.accountTo, t.account, t.date, t.payee, t.notes, t.amount, t.debit, t.credit, c.name'
export const categoriesColumns = `c.name`;
export const categoryBudgetsColumns = `b.dateFrom, b.dateTo, b.amount, c.name`;